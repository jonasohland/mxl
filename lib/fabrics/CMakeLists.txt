if (MXL_FABRICS_OFI)
    add_subdirectory("ofi")
elseif(MXL_FABRICS_UCX)
    message(FATAL_ERROR "ucx support not implemented")
elseif(MXL_FABRICS_NATIVE)
    message(FATAL_ERROR "native fabrics support not implemented")
endif()
